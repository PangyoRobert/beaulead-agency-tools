# AGENTS.md

이 파일은 이 저장소에서 작업하는 Codex, Claude 및 기타 코딩 에이전트가 따라야 할 공통 규칙의 원본이다.

## 프로젝트 목적

`beaulead-agency-tools`는 고객 제안과 대면 미팅에서 사용할 디지털 에이전시용 인터랙티브 아티팩트를 관리하는 Public GitHub Pages 저장소다.

## 공개 저장소 안전 규칙

- 고객 개인정보, 담당자 연락처, 광고 계정 ID, 인증정보, API 키, 계약서, 비공개 성과 데이터, 내부 원가·마진을 커밋하지 않는다.
- 고객 사례에는 공개 승인을 받은 정보 또는 명확히 가공된 샘플 데이터만 사용한다.
- 실제 고객 데이터나 로그인이 필요한 기능은 이 저장소에 구현하지 말고 별도의 비공개 시스템으로 분리한다.
- `.env`, 로컬 캐시, 운영 데이터 파일은 커밋하지 않는다.

## 구조

- `/index.html` — 에이전시 메인 홈페이지와 GNB
- `/video-quote/` — 영상 제작 견적 아티팩트
- `/development-quote/` — 홈페이지·랜딩 수정 및 상세페이지 개발 예상 견적 아티팩트
- `/google-ads-training-quote/` — Google Ads 교육 상품 예상 견적 아티팩트
- `/ad-quote/` — 광고 운영 견적 아티팩트
- `/content-quote/` — 콘텐츠 제작 견적 아티팩트
- `/media-plan/` — 미디어 플랜 아티팩트
- `/campaign-planner/` — 캠페인 플래너
- `/reporting-dashboard/` — 성과 대시보드
- `/client-demos/` — 공개 가능한 고객사별 시연본
- `/shared/` — 공통 스타일, 스크립트, 이미지
- `/docs/` — 운영 문서
- `/scripts/` — 저장소 검증 도구

새 아티팩트는 목적에 맞는 형제 디렉토리의 `index.html`로 만든다. 루트 `index.html`을 개별 아티팩트로 덮어쓰지 않는다.

## 작업 원칙

- `main`은 항상 고객에게 시연 가능한 상태로 유지한다.
- 작업은 `feature/<짧은-기능명>`, 수정은 `fix/<짧은-문제명>` 브랜치에서 시작한다.
- 변경은 Pull Request로 검토한 뒤 `main`에 병합한다. 긴급 상황을 제외하고 `main`에 직접 push하지 않는다.
- 사용자가 제공하지 않은 가격, 예산, 성과 수치나 승인 기준을 임의로 만들지 않는다.
- 고객사·서비스마다 달라질 값은 HTML/JavaScript에 반복 하드코딩하지 말고 해당 아티팩트의 설정 데이터로 분리한다.
- 요청 범위 밖의 기존 아티팩트는 수정하지 않는다.
- 공통 코드를 바꿀 때는 변경 대상이 아닌 아티팩트가 이전과 동일한지 실제 파일 비교 또는 체크섬으로 확인한다.
- 공개 링크, 내부 링크, 모바일 레이아웃, 키보드 접근성을 배포 전에 확인한다.

## 필수 검증

변경 후 아래 명령을 실행한다.

```sh
python3 scripts/validate_site.py
git diff --check
```

변경 대상이 아닌 아티팩트가 있다면 작업 전후 체크섬도 비교한다.

```sh
shasum -a 256 path/to/index.html
```

검증 결과를 Pull Request 설명에 기록한다. 테스트 통과만으로 완료 처리하지 말고 변경된 페이지를 로컬 또는 배포 주소에서 실제로 열어 확인한다.

## 배포

- GitHub Pages는 `main` 브랜치의 루트 디렉토리를 자동 배포한다.
- Pull Request 검증이 통과하고 검토가 끝난 뒤에만 `main`에 병합한다.
- 병합 후 GitHub Pages 배포 완료와 공개 URL의 HTTP 200 응답을 확인한다.
- 대면 미팅에 사용할 확정본은 `demo-YYYY-MM-DD-이름` 형식의 Git 태그로 남긴다.

자세한 작업 및 복구 절차는 `docs/WORKFLOW.md`를 따른다.
